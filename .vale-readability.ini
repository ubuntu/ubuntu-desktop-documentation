# Core settings appear at the top
# (the "global" section).
MinAlertLevel = warning
Packages = write-good, Readability
StylesPath = .vale-styles

# [formats]
# Format associations appear under
# the optional "formats" section.

[*.md]
# Format-specific settings appear
# under a user-provided "glob"
# pattern.
BasedOnStyles = Readability
# AutomatedReadability and ColemanLiau are optimized for speed of computation. Unnecessary.
Readability.AutomatedReadability = NO
Readability.ColemanLiau = NO

